{
  "name": "battery-crm",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/src/index.js",
    "build": "echo 'build done'",
    "postinstall": "npm install --prefix backend"
  },
  "engines": { "node": ">=18.0.0" }
}
